"""A mypy plugin to validate at type checking time that all variants of a union type implements a protocol."""

from ._assert_union_implements import assert_union_implements as assert_union_implements
